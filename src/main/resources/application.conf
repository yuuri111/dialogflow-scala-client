dialogflow {
  project-id = ""
  project-id = ${?PROJECT_ID}
  google-credential = ""
  google-credential = ${?GOOGLE_CREDENTIALS}

  detect-intent {
    text = ""
    text = ${?TEXT}
    session-id = ""
    session-id = ${?SESSION_ID}
    language-code = ""
    language-code = ${?LANGUAGE_CODE}
  }
}
